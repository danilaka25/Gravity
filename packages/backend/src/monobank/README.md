# Monobank Jar Parser Service

Автоматический сервис для парсинга информации о накопленном балансе в Monobank jar каждые 10 минут.

## Установка

```bash
npm install @nestjs/schedule axios
```

## Использование

### Проверить текущий баланс банки

```bash
curl http://localhost:3000/monobank/jar
```

Ответ:
```json
{
  "accumulated": 1234.56,
  "balance": 1234.56,
  "currency": "₴",
  "timestamp": "2026-01-15T15:10:38.667Z"
}
```

## Как это работает

1. **Cron Job** - запускается каждые 10 минут (`EVERY_10_MINUTES`)
2. **Парсинг** - получает данные с URL `https://send.monobank.ua/jar/4goSfKKvPn.json`
3. **Логирование** - выводит накопленную сумму в консоль
4. **API** - доступен эндпоинт для получения текущих данных

## Логи

Каждые 10 минут в консоли появляется:

```
========== MONOBANK JAR DATA ==========
📌 Название банки
💰 Накоплено: 1234.56 ₴
💳 Баланс: 1234.56 ₴
======================================
```

## Конфигурация

Для изменения интервала парсинга, отредактируйте в `monobank.service.ts`:

```typescript
@Cron(CronExpression.EVERY_10_MINUTES)  // Изменить здесь
```

## Доступные интервалы

- `EVERY_SECOND` - каждую секунду
- `EVERY_10_SECONDS` - каждые 10 секунд
- `EVERY_30_SECONDS` - каждые 30 секунд
- `EVERY_MINUTE` - каждую минуту
- `EVERY_10_MINUTES` - каждые 10 минут
- `EVERY_30_MINUTES` - каждые 30 минут
- `EVERY_HOUR` - каждый час
- И другие...
